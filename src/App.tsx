import Home from 'pages/Home/Home'

// tab  key working behind the modal
// Give the outline of the test Button in dark mode
// Quiz + timer - not counting the answers without clicking the next button
// Focus on test button on opening the page
// implement the performance hooks in the project
// Disable the hover color in the quiz after the quiz answer is selected

function App() {
  return (
    <main className="main">
      <Home />
    </main>
  )
}

export default App
